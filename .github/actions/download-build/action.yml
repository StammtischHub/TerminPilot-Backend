name: Download Build Output
description: Downloads the shared build output artifact for the current commit

runs:
  using: composite
  steps:
    - name: Download build output
      uses: actions/download-artifact@v4
      with:
        name: terminpilot-backend-${{ github.sha }}
        path: build/