spring:
  datasource:
    url: jdbc:mysql://localhost:3306/TerminPilot
    username: TerminPilotUser
    password: {DATABASE_USERNAME_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

google:
  calendar:
    credentials-path: classpath:google-credentials.json
    id: {GOOGLE_CALENDAR_ID}